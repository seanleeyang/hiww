# Keep the build context small and the image lean.
node_modules
dist
coverage
.git
.gitignore
.gitattributes
.github
.env
.env.*
.vscode
.idea
.DS_Store

# Not needed to build or run the API
mobile
docs
tests
uploads
*.md
postman
*.postman_collection.json

# admin-web is built in its own Docker stage (see Dockerfile) — a stale
# local build under public/admin must never leak into the image.
public/admin
